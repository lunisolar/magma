name: Build and verify

on:
  # Triggered manually — no automated release runs.
  workflow_dispatch:

jobs:
  java-ci:
    uses: lunisolar/lunisolar-github-workflows/.github/workflows/ci-build-and-verify.maven.2026.yml@main